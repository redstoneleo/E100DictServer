# VoiceToImage 客户端使用说明

## 认证流程

VoiceToImage API 使用与 ChatBot 相同的 JWT Token + HMAC 签名认证机制，采用两步 challenge-response 方式，避免明文传输 `device_secret`。

### 1. 获取 Token（Challenge-Response）

#### 1.1 请求 nonce（challenge）

```http
POST /device/challenge/
Content-Type: application/json

{
    "device_id": "LYY-202603-000123"
}
```

响应：
```json
{
    "nonce": "random_nonce_string"
}
```

#### 1.2 计算签名并换取 token

在设备端计算 HMAC-SHA256 签名（`device_secret` 不上送）：

```python
import hmac
import hashlib

signature = hmac.new(
    device_secret.encode('utf-8'),
    nonce.encode('utf-8'),
    hashlib.sha256
).hexdigest()
```

然后请求 token：

```http
POST /device/token/
Content-Type: application/json

{
    "device_id": "LYY-202603-000123",
    "signature": "hex_hmac_sha256_signature"
}
```

响应：
```json
{
    "access": "eyJ0eXAiOiJKV1QiLCJhbGc...",
    "refresh": "eyJ0eXAiOiJKV1QiLCJhbGc..."
}
```

注意：
- nonce 在服务端缓存时间很短（约 60s），且成功使用后会被删除
- 过期或已使用需要重新获取 nonce

### 2. 刷新 Token

当 access token 过期后，使用 refresh token 换取新的 access token：

```http
POST /api/token/refresh/
Content-Type: application/json

{
    "refresh": "refresh_token"
}
```

响应：
```json
{
    "access": "new_access_token"
}
```

### 3. 调用 API

获取到 access token 后，就可以调用 VoiceToImage API 了。

#### 3.1 上传语音文件

```http
POST /voicetoimage/upload/
Content-Type: multipart/form-data
Authorization: Bearer {access_token}

参数：
- audio: 音频文件（必需）
- style: 图片风格（可选）：简笔画、线稿、素描、动漫、卡通、水墨
```

或者将 token 放在表单参数中：
```http
POST /voicetoimage/upload/
Content-Type: multipart/form-data

参数：
- audio: 音频文件（必需）
- style: 图片风格（可选）
- token: JWT access token（必需）
```

响应：
```json
{
    "status": "success",
    "task_id": 123,
    "message": "Voice recording uploaded and processing started."
}
```

#### 3.2 查询任务状态

```http
GET /voicetoimage/status/{task_id}/
Authorization: Bearer {access_token}
```

或者使用 URL 参数：
```http
GET /voicetoimage/status/{task_id}/?token={access_token}
```

响应：
```json
{
    "id": 123,
    "status": "COMPLETED",
    "style": "卡通",
    "recognized_text": "识别的文字内容",
    "generated_image_url": "https://...",
    "error_message": null,
    "created_at": "2026-03-13T10:00:00Z"
}
```

任务状态说明：
- PENDING: 等待处理
- PROCESSING_STT: 正在进行语音识别
- PROCESSING_IMAGE: 正在生成图片
- COMPLETED: 已完成
- FAILED: 失败

## Python 客户端示例

```python
import requests
import hmac
import hashlib
import time

class VoiceToImageClient:
    def __init__(self, base_url, device_id, device_secret):
        self.base_url = base_url
        self.device_id = device_id
        self.device_secret = device_secret
        self.access_token = None
        self.refresh_token = None
    
    def authenticate(self):
        """两步 challenge-response 获取 token"""
        # 1. 请求 nonce
        response = requests.post(
            f"{self.base_url}/device/challenge/",
            json={"device_id": self.device_id}
        )
        response.raise_for_status()
        nonce = response.json()['nonce']
        
        # 2. 计算签名
        signature = hmac.new(
            self.device_secret.encode('utf-8'),
            nonce.encode('utf-8'),
            hashlib.sha256
        ).hexdigest()
        
        # 3. 换取 token
        response = requests.post(
            f"{self.base_url}/device/token/",
            json={
                "device_id": self.device_id,
                "signature": signature
            }
        )
        response.raise_for_status()
        data = response.json()
        self.access_token = data['access']
        self.refresh_token = data['refresh']
        return data
    
    def refresh_access_token(self):
        """刷新 access token"""
        if not self.refresh_token:
            raise Exception("No refresh token available")
        
        response = requests.post(
            f"{self.base_url}/api/token/refresh/",
            json={"refresh": self.refresh_token}
        )
        response.raise_for_status()
        self.access_token = response.json()['access']
        return self.access_token
    
    def upload_voice(self, audio_file_path, style='卡通'):
        """上传语音文件"""
        if not self.access_token:
            self.authenticate()
        
        with open(audio_file_path, 'rb') as f:
            files = {'audio': f}
            data = {'style': style}
            headers = {'Authorization': f'Bearer {self.access_token}'}
            
            response = requests.post(
                f"{self.base_url}/VoiceToImage/upload/",
                files=files,
                data=data,
                headers=headers
            )
            
            # 如果 token 过期，尝试刷新后重试
            if response.status_code == 401:
                self.refresh_access_token()
                headers = {'Authorization': f'Bearer {self.access_token}'}
                f.seek(0)  # 重置文件指针
                response = requests.post(
                    f"{self.base_url}/VoiceToImage/upload/",
                    files={'audio': f},
                    data=data,
                    headers=headers
                )
        
        response.raise_for_status()
        return response.json()
    
    def get_task_status(self, task_id):
        """查询任务状态"""
        if not self.access_token:
            self.authenticate()
        
        headers = {'Authorization': f'Bearer {self.access_token}'}
        response = requests.get(
            f"{self.base_url}/VoiceToImage/status/{task_id}/",
            headers=headers
        )
        
        # 如果 token 过期，尝试刷新后重试
        if response.status_code == 401:
            self.refresh_access_token()
            headers = {'Authorization': f'Bearer {self.access_token}'}
            response = requests.get(
                f"{self.base_url}/VoiceToImage/status/{task_id}/",
                headers=headers
            )
        
        response.raise_for_status()
        return response.json()

# 使用示例
if __name__ == '__main__':
    client = VoiceToImageClient(
        base_url='http://127.0.0.1:8000',
        device_id='FA1-202603-000123',
        device_secret='381570a6a34e98ea6183687f0932ed938436323be216bab360f9ac2376c2edbe'
    )
    
    # 上传语音
    result = client.upload_voice(r'F:\BaiduNetdiskDownload\ConsumerSoftwareProject\E100DictServer\media\voice_recordings\书包.pcm', style='卡通')
    print(f"Upload result: {result}")
    
    if result['status'] == 'success':
        task_id = result['task_id']
        
        # 轮询查询状态
        while True:
            status = client.get_task_status(task_id)
            print(f"Task status: {status['status']}")
            
            if status['status'] in ['COMPLETED', 'FAILED']:
                print(f"Final result: {status}")
                if status['status'] == 'COMPLETED':
                    print(f"Recognized text: {status['recognized_text']}")
                    print(f"Image URL: {status['generated_image_url']}")
                else:
                    print(f"Error: {status['error_message']}")
                break
            
            time.sleep(2)

```

## 错误码说明

- 400: 请求参数错误（缺少音频文件等）
- 401: 认证失败（token 无效或过期）
- 403: 权限不足（设备未激活或访问其他设备的任务）
- 404: 资源不存在（设备或任务不存在）
- 405: 请求方法错误

## 注意事项

1. 使用两步 challenge-response 方式获取 token，`device_secret` 永远不会在网络上传输
2. Nonce 在服务端缓存时间很短（约 60s），且成功使用后会被删除
3. Access token 有过期时间，过期后使用 refresh token 刷新
4. 只能查询自己设备创建的任务，无法访问其他设备的任务
5. 音频文件格式建议使用 WAV、MP3 等常见格式
6. 生产环境务必使用 HTTPS 协议

## 安全建议

1. 妥善保管 `device_secret`，不要硬编码在客户端代码中
2. 使用 HTTPS/WSS 加密传输
3. 定期轮换 refresh token
4. 监控异常的 API 调用频率
